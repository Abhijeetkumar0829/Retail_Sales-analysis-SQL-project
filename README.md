# Retail_Sales-analysis-SQL-project
In this project basically we have done a analysis on retail sales data using an sql. we created a new database and after creating database we have imported a dataheet from excel which was basicall a CSV file. after importing csv file into our sql server workspace first we cleaned the data of a table than perform some analysis.
